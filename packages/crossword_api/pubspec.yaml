name: crossword_api
description: Backend API client for crossword generation (ikors.se).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  equatable: ^2.0.7
  crossword_core:
    path: ../crossword_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
